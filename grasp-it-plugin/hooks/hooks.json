{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "printf '%s' \"$TOOL_INPUT\" | grep -qE 'git\\s+(commit|merge|cherry-pick|rebase)' && [ -f .grasp-it/config.json ] && grep -q '\"autoUpdate\".*true' .grasp-it/config.json && echo \"[grasp-it] Commit detected with auto-update enabled. You MUST read the file at ${CLAUDE_PLUGIN_ROOT}/hooks/auto-update-prompt.md and execute its instructions to incrementally update the knowledge graph. Do not ask the user for confirmation — just do it.\" || true"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ -f .grasp-it/config.json ] && grep -q '\"autoUpdate\".*true' .grasp-it/config.json && echo \"[grasp-it] Auto-update is enabled. The knowledge graph will be updated incrementally on commits. You can also manually run /grasp to perform a full analysis.\" || true"
          }
        ]
      }
    ]
  }
}